import 'package:flutter/material.dart';

/// Warna status smoke test. Sumber tunggal agar konsisten di seluruh UI.
class AppColors {
  const AppColors._();

  static const Color seed = Color(0xFF3F51B5);

  static const Color ok = Color(0xFF2E7D32);
  static const Color fail = Color(0xFFE53935);
  static const Color skip = Color(0xFFFFA000);
  static const Color pending = Color(0xFF9E9E9E);
}
