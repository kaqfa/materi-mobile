name: p06_testing_device
description: Remedial Task Tracker — P06 Device feature (image picker + gallery fallback + permission), unit & widget test targets. Inti wiring attachment ke provider/UI sengaja TODO; fallback device-unavailable/permission-denied sudah hijau.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.4.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
