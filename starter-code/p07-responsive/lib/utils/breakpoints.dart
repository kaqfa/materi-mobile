/// Breakpoint terpusat — satu sumber kebenaran untuk ambang layout.
library;

abstract final class Breakpoints {
  /// Di atas nilai ini: dua kolom / master-detail.
  static const tablet = 600.0;

  /// Di atas nilai ini: layout tiga kolom (desktop/tablet besar).
  static const desktop = 1024.0;
}
