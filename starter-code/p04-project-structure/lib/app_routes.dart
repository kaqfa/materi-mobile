/// Pusat nama route — hindari string tersebar di banyak file.
///
/// Named routes + onGenerateRoute memudahkan navigasi saat app membesar
/// (dan dipakai capstone-mu nanti).
class AppRoutes {
  static const home = '/';
  static const taskList = '/tasks';

  // TODO(student) P04-3: tambahkan konstanta untuk route `/tasks/new`
  // dan daftarkan di onGenerateRoute (lihat main.dart).
}
